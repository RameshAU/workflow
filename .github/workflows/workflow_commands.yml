name: wworkflow commands
on: [push]

jobs:
  variable:
    runs-on: ubuntu-latest
    steps:
      - name: Set env variable
        run: echo ENV=five >> $GITHUB
      - name: check env variable
        run: echo $ENV
      
      - name: status
        if: ${{ success() }}
        run: echo succeeded